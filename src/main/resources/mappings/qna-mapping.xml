<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="QnaMapper">

	<insert id = "insertQna">
		INSERT INTO qna(qna_seq, member_Id, qna_Title, qna_Message)
		VALUES(qna_seq.nextval, #{member_Id}, #{qna_Title}, #{qna_Message})
	</insert>
	
	<select id = "getMyQna" resultType = "qna">
		SELECT * FROM qna WHERE member_Id = #{member_Id}
		ORDER BY qna_Done DESC, qna_Seq DESC
	</select>

</mapper>
